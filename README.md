# HorriblesubsScheduleFetcher
A C#/.NET library to facilitate fetching anime simulcast schedules from horriblesubs.info.

## Usage

    var fetcher = new HorriblesubsScheduleFetcher.Fetcher();
    var items = fetcher.Fetch();
